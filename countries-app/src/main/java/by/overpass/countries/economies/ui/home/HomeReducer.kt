package by.overpass.countries.economies.ui.home

import by.overpass.countries.redux.Reducer

class HomeReducer : Reducer<HomeState, HomeAction> {

    override fun reduce(state: HomeState, action: HomeAction): HomeState = state
}